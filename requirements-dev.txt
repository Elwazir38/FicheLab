# Dépendances de développement de FicheLab.
# Installe d'abord les dépendances de prod, puis celles-ci :
#   .\.venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=9.0
